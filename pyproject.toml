[project]
name = "mcp-remote"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sanjit Chaudhary", email = "sanjitchaudhary506@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastmcp>=3.4.5",
]

[project.scripts]
mcp-remote = "mcp_remote:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
